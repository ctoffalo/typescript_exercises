# version: "3.8"
services:
  app:
    build: .
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
    command: ["bash"]
    stdin_open: true
    tty: true
    environment:
      - NODE_ENV=development
    ports:
      - "3000:3000"
