version: "3.8"
services:
  app:
    image: node:20-bullseye-slim
    working_dir: /app
    volumes:
      - .:/app
    command: ["npm", "run", "dev"]
    stdin_open: true
    tty: true
    environment:
      - NODE_ENV=development
    ports:
      - "3000:3000"
